@model IndexVehiclesVM

<p id="my-p">
    <a asp-action="Create" class="btn" style="background-color:darkorange">Create New</a>
</p>

<table class="table table-hover table-striped">
    <thead class="thead-dark">
        <tr>
            <th>
                Brand
            </th>
            <th>
                Model
            </th>
            <th>
                Year
            </th>
            <th>
                Price per day
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Vehicles)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Brand)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Model)
                </td>

                <td>
                    @Html.DisplayFor(modelItem => item.Year)
                </td>

                <td>
                    @Html.DisplayFor(modelItem => item.PricePerDay) лв
                </td>
                <td>                   
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-info">Hui</a>                 
                </td>
            </tr>
        }
    </tbody>
</table>
<partial name="_PagingPartial" />
